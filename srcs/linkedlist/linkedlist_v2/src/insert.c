#include "../include/list.h"

int insert(struct list* const l, const int index, const void* const data)
{
    return 0;
}

int insert_first(struct list* const l, const void* const data)
{
    return 0;
}

int insert_last(struct list* const l, const void* const data)
{
    return 0;
}