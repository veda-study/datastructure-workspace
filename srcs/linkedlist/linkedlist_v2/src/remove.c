#include "../include/list.h"

#include <stdio.h>

void* remove_at(struct list* const l, const int index)
{
    return NULL;
}

void* remove_first(struct list* const l)
{
    return NULL;
}

void* remove_last(struct list* const l)
{
    return NULL;
}